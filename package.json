{
  "name": "ai-agent-anthropic-template",
  "version": "0.0.1",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsup --config tsup.config.ts",
    "test": "vitest --testTimeout=60000",
    "publish-agent": "npm run build && tsx scripts/publish.ts",
    "set-secrets": "tsx scripts/setSecrets.ts"
  },
  "dependencies": {
    "@phala/wapo-env": "^0.9.4",
    "hono": "^4.5.6"
  },
  "devDependencies": {
    "@types/node": "^22.1.0",
    "dotenv": "^16.4.5",
    "thirdweb": "^5.32.3",
    "tsup": "^8.2.4",
    "tsx": "^4.7.1",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
